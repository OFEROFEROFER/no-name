import { ExtractTrailerPipe } from './extract-trailer.pipe';

describe('ExtractTrailerPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractTrailerPipe();
    expect(pipe).toBeTruthy();
  });
});
